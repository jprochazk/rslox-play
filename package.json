{
    "scripts": {
        "dev": "snowpack dev",
        "build": "(snowpack build && node postbuild.js)",
        "build:lib": "(cd lib && wasm-pack build --release --out-name index --target web)",
        "build:lib:dev": "(cd lib && wasm-pack build --dev --out-name index --target web)"
    },
    "dependencies": {
        "lodash": "^4.17.21",
        "monaco-editor": "^0.23.0",
        "svelte": "^3.24.0"
    },
    "devDependencies": {
        "@snowpack/plugin-dotenv": "^2.0.5",
        "@snowpack/plugin-svelte": "^3.5.0",
        "@snowpack/plugin-typescript": "^1.2.0",
        "@snowpack/plugin-webpack": "^2.3.1",
        "@tsconfig/svelte": "^1.0.10",
        "@types/lodash": "^4.14.168",
        "@types/snowpack-env": "^2.3.2",
        "snowpack": "^3.0.1",
        "svelte-preprocess": "^4.0.8",
        "typescript": "^4.0.0"
    }
}
